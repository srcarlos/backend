ngDescribe({
    name: 'Test impuesto-list component',
    modules: 'app',
    element: '<impuesto-list></impuesto-list>',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
