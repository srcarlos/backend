ngDescribe({
    name: 'Test tipomovimiento-detail component',
    modules: 'app',
    element: '<tipomovimiento-detail></tipomovimiento-detail>',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
