ngDescribe({
    name: 'Test transferencia-add component',
    modules: 'app',
    element: '<transferencia-add></transferencia-add>',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
