ngDescribe({
    name: 'Test bodega-detail component',
    modules: 'app',
    element: '<bodega-detail></bodega-detail>',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
