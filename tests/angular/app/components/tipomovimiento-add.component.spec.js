ngDescribe({
    name: 'Test tipomovimiento-add component',
    modules: 'app',
    element: '<tipomovimiento-add></tipomovimiento-add>',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
