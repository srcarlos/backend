ngDescribe({
    name: 'Test proveedor-add component',
    modules: 'app',
    element: '<proveedor-add></proveedor-add>',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
