ngDescribe({
    name: 'Test ingrediente-add component',
    modules: 'app',
    element: '<ingrediente-add></ingrediente-add>',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
