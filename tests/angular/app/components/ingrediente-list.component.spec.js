ngDescribe({
    name: 'Test ingrediente-list component',
    modules: 'app',
    element: '<ingrediente-list></ingrediente-list>',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
