ngDescribe({
    name: 'Test plato-add component',
    modules: 'app',
    element: '<plato-add></plato-add>',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
