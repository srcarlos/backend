ngDescribe({
    name: 'Test ordencompra-transferir component',
    modules: 'app',
    element: '<ordencompra-transferir></ordencompra-transferir>',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
