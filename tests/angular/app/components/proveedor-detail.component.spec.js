ngDescribe({
    name: 'Test proveedor-detail component',
    modules: 'app',
    element: '<proveedor-detail></proveedor-detail>',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
