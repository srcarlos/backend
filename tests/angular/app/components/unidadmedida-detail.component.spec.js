ngDescribe({
    name: 'Test unidadmedida-detail component',
    modules: 'app',
    element: '<unidadmedida-detail></unidadmedida-detail>',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
