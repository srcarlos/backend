ngDescribe({
    name: 'Test unidadmedida-add component',
    modules: 'app',
    element: '<unidadmedida-add></unidadmedida-add>',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
