ngDescribe({
    name: 'Test unidadmedida-edit component',
    modules: 'app',
    element: '<unidadmedida-edit></unidadmedida-edit>',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
