ngDescribe({
    name: 'Test ingrediente-detail component',
    modules: 'app',
    element: '<ingrediente-detail></ingrediente-detail>',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
