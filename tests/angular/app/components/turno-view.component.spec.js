ngDescribe({
    name: 'Test turno-view component',
    modules: 'app',
    element: '<turno-view></turno-view>',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
