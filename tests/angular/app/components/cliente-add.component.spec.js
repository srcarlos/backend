ngDescribe({
    name: 'Test cliente-add component',
    modules: 'app',
    element: '<cliente-add></cliente-add>',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
