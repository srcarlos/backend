ngDescribe({
    name: 'Test impuestos-add component',
    modules: 'app',
    element: '<impuestos-add></impuestos-add>',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
