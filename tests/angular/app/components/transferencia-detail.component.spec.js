ngDescribe({
    name: 'Test transferencia-detail component',
    modules: 'app',
    element: '<transferencia-detail></transferencia-detail>',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
