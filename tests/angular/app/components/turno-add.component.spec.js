ngDescribe({
    name: 'Test turno-add component',
    modules: 'app',
    element: '<turno-add></turno-add>',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
