ngDescribe({
    name: 'Test unidadmedida-list component',
    modules: 'app',
    element: '<unidadmedida-list></unidadmedida-list>',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
