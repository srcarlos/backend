ngDescribe({
    name: 'Test impuestos-detail component',
    modules: 'app',
    element: '<impuestos-detail></impuestos-detail>',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
